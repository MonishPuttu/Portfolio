# ── API ─────────────────────────────────────────
# Required. The production build fails without it (see vite.config.js), because
# a bundle with no API URL looks fine and silently loads nothing.
VITE_API_URL=http://localhost:5000/api
